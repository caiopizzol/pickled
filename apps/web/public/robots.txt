# Pickled landing robots.txt.
#
# Cloudflare's AI Audit feature prepends its managed robots content
# (Content-Signals preamble + AI-training-bot blocklist) to this
# file at the edge. We don't repeat those rules here to avoid
# duplicate User-agent groups in the served response. The bot
# blocklist (Amazonbot, Applebot-Extended, Bytespider, CCBot,
# ClaudeBot, CloudflareBrowserRenderingCrawler, Google-Extended,
# GPTBot, meta-externalagent) is owned by Cloudflare AI Audit; manage
# it there. This file only exists to defeat Vite's SPA catch-all,
# which was otherwise serving index.html for /robots.txt and
# breaking strict robots parsers with appended HTML.

User-agent: *
Allow: /
